part of 'package:catat_uang/import_url_file.dart';

class LayoutTheme {
  static allRoundedRect({required double? radius}) =>
      RoundedRectangleBorder(borderRadius: BorderRadius.circular(radius!.r));
}
